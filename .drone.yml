kind: pipeline
name: default
type: build
steps:
-      name: build
       image:  plugins/docker
       commands: 
       -  "docker build -t node ."
       when:
          branch:
              -   master