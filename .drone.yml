kind: pipeline
name: default
type: docker
steps:
-      name: build
       image:  plugins/docker
       commands: 
       -  "docker build -t node ."
       when:
          branch:
              -   master